<div class="pt-4 pb-4">
  <div class="row">
    <div class="col-md-8 offset-md-2">
      <div class="d-flex">
        <h1>Editing Category</h1>
        <div class="ml-auto">
          <%= link_to 'Show', @category, class: 'btn btn-outline-primary mr-2' %>
          <%= link_to 'Back', categories_path, class: 'btn btn-outline-secondary' %>
        </div>
      </div>
      <%= render 'form', category: @category %>
    </div>
  </div>
</div>
